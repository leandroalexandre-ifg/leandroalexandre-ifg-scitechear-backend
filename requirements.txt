# ============================================================
# SciTech Ear — Backend · dependências
# ============================================================

# --- API (Fase 1) ---
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.4
pydantic-settings==2.11.0
python-multipart==0.0.20

# --- Testes ---
pytest==8.4.2
httpx==0.28.1

# --- Pipeline de IA (a instalar por fase, só no runtime com GPU) ---
# torch                    # Fase 3+ (WhisperX, SpeechBrain)
# torchaudio               # Fase 3+
# whisperx                 # Fase 3 — TranscriptionService
# pyannote.audio           # Fase 4 — DiarizationService
# speechbrain              # Fase 4 — VoiceIdentityService / VoiceEnrollmentService
# requests                 # Fase 5 — cliente HTTP da API do Ollama
