# SciTech Ear - proxy TLS na frente da API
#
# Motivo de existir: a API fala HTTP puro e fica em 127.0.0.1:18080. Para o
# piloto alguem precisa escutar na rede, e nao pode ser a API - senha e audio
# de reuniao viajariam em claro. Este proxy termina o TLS e repassa em
# loopback. A API NUNCA escuta na rede; ver docs/DEPLOY.md e docs/TLS.md.
#
# 2026-09-21: a maquina deixou de estar atras do NAT do IFG. A eno1 tem agora
# 200.17.57.229/28 PUBLICO e roteavel (gateway 200.17.57.225), configurado
# estaticamente pelo admin - nao ha lease DHCP. O endereco antigo,
# 10.4.254.201, NAO EXISTE MAIS nesta maquina: todo artefato que o citava
# (SAN do certificado, default_sni, --dart-define do app) virou obsoleto de
# uma vez.
#
# 2026-09-21, mais tarde: a borda do IFG liberou a entrada na porta 443, e o
# proxy saiu do loopback. Escolher a 443 nao foi preferencia - foi o unico
# jeito de dispensar o CTI, porque a borda filtra POR PORTA e a 443 era a
# candidata ja permitida. O preco e que 443 e porta privilegiada; como ele foi
# pago sem virar o servico de root esta no bloco do site, abaixo.
#
# Copia implantada: este mesmo arquivo, lido direto do checkout pela unidade
# scitechear-proxy.service (mesma logica dos outros servicos - a branch com
# checkout e o que roda).
{
	# CA local e certificados FORA do checkout, pelo mesmo motivo de storage/
	# e hf-cache: um `git clean` ou re-clone nao pode levar a chave da CA
	# embora. Se ela sumir, todo app ja instalado para de confiar no servidor
	# e precisa de nova build - a chave e o que ha de mais caro de perder aqui.
	storage file_system /data/projects/leandro/scitechear/caddy

	# Sem root: por padrao o Caddy tentaria instalar a raiz no truststore do
	# sistema (pede sudo). Nao e preciso - o app Android carrega a raiz
	# empacotada nele via network_security_config, e o truststore da maquina
	# compartilhada nao e problema nosso para mexer.
	skip_install_trust

	# O endpoint de administracao (default 127.0.0.1:2019) reconfigura o Caddy
	# sem autenticacao. Nada aqui precisa dele; a maquina e compartilhada.
	#
	# PRECO DISSO, descoberto em 2026-09-21: `caddy reload` aplica configuracao
	# FALANDO COM ESSA ADMIN API. Com ela desligada, o ExecReload da unidade
	# sempre falha ("dial tcp 127.0.0.1:2019: connect: connection refused") e o
	# servico segue no ar com a configuracao ANTIGA - falha silenciosa do pior
	# tipo, porque o systemd reporta erro mas o proxy continua respondendo.
	# Para aplicar mudanca daqui use `systemctl --user restart`, que derruba
	# conexao. Ver docs/TLS.md, secao Operacao, inclusive a alternativa de
	# admin em socket unix (nao adotada; e decisao de postura de seguranca).
	admin off

	# Um listener so, uma regra de firewall so: sem redirect 80->443 (que
	# abriria a porta 80) e sem HTTP/3 (que pediria UDP alem do TCP ao admin).
	# O cliente e um app Dart/Android, que nao fala h3 mesmo.
	auto_https disable_redirects
	servers {
		protocols h1 h2
	}

	# Cliente que fala com um ENDERECO IP nao manda SNI - a RFC 6066 proibe
	# nome que seja endereco. Sem esta linha o Caddy nao acha o site pelo SNI
	# e o handshake morre com "tlsv1 alert internal error", antes de qualquer
	# resposta HTTP. Verificado em 2026-09-06; e o erro que mais custa a
	# diagnosticar aqui, porque nao parece problema de certificado.
	#
	# Este linha sai sozinha no dia em que houver nome DNS - ela so existe
	# porque o cliente fala com um IP.
	default_sni 200.17.57.229
}

# O endereco do site define a IDENTIDADE do certificado (o SAN) e a porta em
# que se escuta; o `bind` abaixo decide em qual INTERFACE. A porta nao entra
# no certificado, entao sair da 18443 para a 443 nao exigiu reemitir nada - a
# folha continua com `IP:200.17.57.229` como unico SAN.
https://200.17.57.229:443 {
	# NA REDE desde 2026-09-21. Quem filtrava era a BORDA DO IFG, nunca esta
	# maquina: o ufw daqui esta DESLIGADO (`ENABLED=no` em /etc/ufw/ufw.conf),
	# e a prova sem root e que uma porta sem ouvinte recusa a conexao
	# INSTANTANEAMENTE pelo IP publico - regra DROP daria travamento. Com a
	# borda liberando a 443, o piloto deixou de depender do CTI e do admin.
	#
	# O QUE ESTE BIND PUBLICA, dito em voz alta porque nao se ve daqui: o
	# registro de contas esta deliberadamente ABERTO a qualquer e-mail
	# (AUTH_ALLOWED_EMAIL_DOMAINS vazia no .env). Era decisao inofensiva
	# enquanto a unica entrada era o tunel SSH; Leandro a reafirmou em
	# 2026-09-21 ja sabendo do IP publico. Daqui em diante "aberto" significa
	# qualquer pessoa DA INTERNET, e o unico amortecedor que restou e o rate
	# limit de registro (10/h por IP), dimensionado para um universo
	# institucional em que um IP ainda identificava alguem.
	#
	# PRECO DA 443: e porta privilegiada, e este proxy roda como unidade de
	# USUARIO, sem root. Duas coisas destravam isso, e sao inseparaveis:
	#
	#   sudo setcap cap_net_bind_service=+ep ~/.local/bin/caddy
	#   + remover `NoNewPrivileges=yes` da unidade
	#
	# Sozinho, o setcap NAO TEM EFEITO: com NoNewPrivileges o kernel ignora
	# capability de arquivo. A falha e `permission denied` no bind, que nao
	# sugere a causa. E a capability mora no INODE do binario: atualizar o
	# Caddy a apaga, e o servico entra em loop de restart no proximo boot.
	# Ver deploy/scitechear-proxy.service e docs/TLS.md.
	#
	# Use 0.0.0.0, NUNCA 200.17.57.229 literal. O endereco e estatico (posto
	# no netplan pelo admin), o que torna o bind num IP tecnicamente possivel
	# - mas ele continua sendo a escolha errada: amarra o servico a uma
	# decisao do admin que pode mudar sem nos avisar, e a falha resultante e
	# `cannot assign requested address` em loop de restart, no boot, sem
	# ninguem olhando.
	bind 0.0.0.0

	# CA local do proprio Caddy: raiz de 10 anos, folha de 12h renovada
	# sozinha enquanto o servico roda. E o que dispensa reemitir certificado
	# na mao - inclusive quando o IP do DHCP mudar, porque a raiz (que e o que
	# o app confia) nao muda junto.
	tls internal

	# O X-Forwarded-For nao precisa de configuracao aqui, e isso e menos obvio
	# do que parece. Desde o Caddy 2.7 a lista `trusted_proxies` comeca VAZIA,
	# entao um X-Forwarded-For que o CLIENTE mandar e descartado, nao anexado -
	# medido em 2026-09-06: forjando `X-Forwarded-For: 10.4.1.99` o upstream
	# recebeu so o endereco real. Do outro lado, o uvicorn confia em 127.0.0.1
	# por default (--forwarded-allow-ips) e reescreve request.client a partir
	# do cabecalho - tambem medido, com um cliente vindo de 127.0.0.2. As duas
	# pontas juntas mantem o rate limiting de /auth por cliente real, que e o
	# que um proxy costuma quebrar silenciosamente (todo mundo virando
	# 127.0.0.1 num balde so). Nao adicione `header_up X-Forwarded-For` aqui:
	# alem de redundante, o Caddy avisa que e desnecessario no `validate`.
	reverse_proxy 127.0.0.1:18080

	log {
		# O WebSocket de progresso autentica por query string
		# (/ws/{job_id}?token=...) porque o handshake nao aceita header
		# Authorization em todo cliente. Sem este filtro, cada conexao
		# escreveria um JWT valido no journald - log nao e lugar de credencial.
		format filter {
			wrap console
			fields {
				request>uri query {
					replace token REDACTED
				}
			}
		}
	}
}
